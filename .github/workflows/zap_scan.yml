name: OWASP ZAP Security Scan

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  zap_scan:
    runs-on: ubuntu-latest
    name: Quét lỗ hổng bảo mật Web
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.12.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          target: 'https://demo.owasp-juice.shop/'
          fail_action: false
          allow_issue_writing: false  # <--- DÒNG NÀY ĐỂ DIỆT LỖI 128
          cmd_options: '-r report.html'

      - name: Di chuyển báo cáo vào thư mục
        if: always()
        run: |
          mkdir -p 02_Ket_qua_quet
          if [ -f "report.html" ]; then
            cp report.html 02_Ket_qua_quet/report_$(date +'%Y%m%d_%H%M').html
          fi
          
      - name: Auto Commit and Push
        if: always()
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Auto-update: Báo cáo bảo mật ${{ github.run_id }}"
          file_pattern: '02_Ket_qua_quet/*.html'